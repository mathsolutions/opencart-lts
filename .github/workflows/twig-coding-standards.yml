name: twig coding standards
on: [ push, pull_request ]

jobs:
  coding-standard:
    runs-on: ubuntu-latest

    name: Coding Standard

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Cache tools
        uses: actions/cache@v5
        with:
          path: .cache/twig-cs-fixer.cache
          key: ${{ runner.os }}-twig-cs-fixer-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-twig-cs-fixer-

      - name: PHP PHP-CS-Fixer
        run: composer twig-cs-check
